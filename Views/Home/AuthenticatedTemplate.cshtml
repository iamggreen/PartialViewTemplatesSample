@{ 
    var message = string.IsNullOrEmpty(ViewBag.Name)
                 ? "You are not logged in."
                 : "Hello, " + ViewBag.Name + ".  You can see extra stuff because you are logged in.";
                 }

@message